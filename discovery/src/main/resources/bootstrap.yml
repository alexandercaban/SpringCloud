spring:
  cloud:
    config:
      name: discovery
      discovery:
        service-id: config
        enabled: true
      uri: http://localhost:8081
      username: configUser
      password: configPassword